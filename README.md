# ToDo-Bootstrap-Nodejs

A simple Todo List web application that uses bootstrap and ejs templating for front-end and uses Node.js
and Express for backend. The todo items are stored on a MongoDB database.
